Lax Friedrich

Enter in file 'Lax_Friedrich.m' change the input 'data' to the one that you want to verify

Roe lineariziation

To obtain the results of the Roe linearization you need to open the file 'Roe_method.m' and change  the input 'data'

Errors

The plot of the errors are generated by the files 'ErrorLFcase*.m' or 'ErrorROEcase*.m', launch the file and it will plot the order
of the errors
